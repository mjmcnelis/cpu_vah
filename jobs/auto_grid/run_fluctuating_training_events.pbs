#!/bin/bash -l
#PBS -l walltime=72:00:00,nodes=1:ppn=1,mem=1gb
#PBS -j oe
#PBS -A PAS0254

cd $HOME/cpu_vah

for((n = 1; n <= $EVENTS; n++))
do
	echo
	echo "Running event $n"
	./cpu_vah $JOBID
	echo
done